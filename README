Program to convert output of Dataflow to LCIO format

To use, first edit the LCIO line of "env.sh" to point towards your installation.

Source env.sh, run make and execute.

Arguments :

./dataflow2lcio {input=treeFile.root} {output=outFile} {Number of rocs = 8}